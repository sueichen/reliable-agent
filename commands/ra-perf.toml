description = "在怀疑性能问题、需要白盒深度优化时使用"

prompt = """
Invoke the ra-perf skill. Follow the hotspot-driven deep iteration workflow defined in SKILL.md:

Phase 1: Establish baseline (perf stat + time)
Phase 2: Discover hotspots (perf record + report)
Phase 3: Deep-dive hotspot (perf list → targeted perf stat → perf annotate)
Phase 4: Understand the code (algorithm, data layout, redundancy)
Phase 5: Optimize + verify (code/compiler/runtime → test suite → before/after)
Loop back to Phase 2. At least 5 rounds of iteration until IPC saturates or hotspots are exhausted.
"""
