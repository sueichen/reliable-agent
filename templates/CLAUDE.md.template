# CLAUDE.md — 项目宪法模板

> 此文件由 `/spec-reliable` 命令生成。它是项目的宪法——定义了代码怎么写、怎么测试、怎么提交、什么算"完成"。
> 每次新 session 启动时，AI 代理会先读取此文件作为行为准则。

## 项目信息

- **项目名称**: [project-name]
- **项目描述**: [简短描述]
- **技术栈**: [语言/框架/运行时]

## 命令

```bash
# 构建
npm run build

# 测试（含覆盖率）
npm test -- --coverage

# Lint
npm run lint

# 类型检查
npx tsc --noEmit

# 开发运行
npm run dev
```

## 项目结构

```
src/           # 源代码
tests/         # 测试代码
docs/          # 文档
  plans/       #   实现计划
  adrs/        #   架构决策记录
```

## 代码风格

### 命名
- [语言惯例，例如：变量 camelCase，类 PascalCase，常量 UPPER_SNAKE_CASE]

### 代码示例
```typescript
// 正面例子：清晰的命名，单一职责
export function calculateShippingCost(order: Order, destination: Address): number {
  // ...
}
```

## 测试策略

### 框架与工具
- 测试框架: [jest / vitest / pytest / ...]
- 覆盖率阈值: [百分比，例如 80%]

### 测试层级
- **单元测试**: 占 80%，测试纯逻辑和公共接口
- **集成测试**: 占 15%，测试模块间交互
- **端到端测试**: 占 5%，测试关键用户流程

### 测试规则
- 每个 bug 修复必须先写复现测试
- 测试行为（behavior），不测试实现（implementation）
- 测试名读起来像规格说明
- 测试之间独立，无共享可变状态

## 边界

### Always（总是做）:
- 提交前运行完整测试套件
- 验证系统边界处的所有输入
- 使用结构化 JSON 日志并带有关联 ID
- 遵循 commit 格式: `[type]([scope]): [description]`
- 在 `/request-review-reliable` 前运行 `/verify-reliable`

### Ask First（先问）:
- 添加新依赖
- 修改数据库 schema
- 修改 CI/CD 配置
- 对现有模块的架构变更
- 删除或重命名公共 API

### Never（绝不做）:
- 提交密钥、令牌或凭证
- 未经人类批准删除失败的测试
- 跳过工作流中的质量门禁
- 未经 `/ship-reliable` 门禁直接部署到生产

## 安全基线

- [ ] OWASP Top 10 防护措施到位
- [ ] 所有用户输入在系统边界处验证
- [ ] 密钥通过环境变量管理，不进版本控制
- [ ] 认证/授权在每个受保护端点检查
- [ ] 依赖项定期审计 CVE
- [ ] LLM 输出视为不可信输入（如适用）

## 性能基线

- [ ] API 响应时间 P95 < [目标毫秒]
- [ ] 内存使用在正常负载下 < [目标]
- [ ] 无 N+1 查询模式
- [ ] 大列表有分页
- [ ] 连接池正确配置

## Commit 格式

```
[type]([scope]): [简洁描述]

[可选的详细描述]

Co-Authored-By: [name] <[email]>
```

**类型**: feat, fix, refactor, test, docs, chore, perf, security, revert

## 文档标准

- README 包含：项目用途、安装步骤、基本用法
- 每个非平凡的公共 API 有 JSDoc/TSDoc
- 架构决策记录在 `docs/adrs/` 中，格式 `YYYY-MM-DD-[标题].md`
- 每个 ADR 记录：背景、决策、后果、考虑的替代方案
